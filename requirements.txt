# Cloud Decoded — Python Backend Dependencies
# Install: pip install -r requirements.txt
#
# REQUIRES Python 3.11+. langgraph's async node execution only passes
# contextvars into asyncio.create_task() correctly on 3.11+ (ASYNCIO_ACCEPTS_
# CONTEXT in langgraph/utils/runnable.py). On 3.10, the HITL interrupt() call
# in core/engine.py's hitl_gate node fails with "RuntimeError: Called
# get_config outside of a runnable context" — every agent's safety pause
# silently breaks. Confirmed root cause 2026-07-14, fixed by installing
# python3.11 (deadsnakes PPA) and rebuilding .venv against it — do not
# rebuild this venv against python3.10 again.

# API framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Agent orchestration
langgraph==0.2.76
langchain-core>=0.2.43,<0.4.0

# HTTP client
httpx==0.27.2

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Environment
python-dotenv==1.0.1

# Database
supabase==2.7.4
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.36

# Billing
stripe==10.12.0

# Auth / crypto
python-jose[cryptography]==3.3.0
cryptography==43.0.1

# Caching / state
redis==5.1.1

# LangGraph Postgres checkpointer (uses psycopg, separate from asyncpg)
langgraph-checkpoint-postgres==2.0.4
psycopg[binary,pool]==3.2.3

# Rate limiting
slowapi==0.1.9

# LLM providers (router deps — do not call directly in agent code)
anthropic==0.34.2
openai==1.52.0
boto3==1.35.36
google-cloud-aiplatform==1.69.0

# YAML config
pyyaml==6.0.2

# Dev / testing
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
